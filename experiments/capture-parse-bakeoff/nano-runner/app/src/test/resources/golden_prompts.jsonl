{"id": "ph-01", "source": "typed", "raw": "Email Dana the Q3 roadmap draft before Thursday standup", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nEmail Dana the Q3 roadmap draft before Thursday standup\n```\n"}
{"id": "ph-03", "source": "typed", "raw": "the thing with the fly deploy token", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nthe thing with the fly deploy token\n```\n"}
{"id": "ph-04", "source": "dictated", "raw": "remind me to water the plants when I get home", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nremind me to water the plants when I get home\n```\n"}
{"id": "ph-05", "source": "typed", "raw": "buy a birthday card for mom, book the oil change, and cancel the gym trial before it charges me", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nbuy a birthday card for mom, book the oil change, and cancel the gym trial before it charges me\n```\n"}
{"id": "ph-06", "source": "typed", "raw": "quarterly taxes", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nquarterly taxes\n```\n"}
{"id": "ph-08", "source": "typed", "raw": "Draft the ADR for the runner endpoint — separate Fly app, not on the sweeper, single bearer token, revisit after the bake-off", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nDraft the ADR for the runner endpoint — separate Fly app, not on the sweeper, single bearer token, revisit after the bake-off\n```\n"}
{"id": "ph-11", "source": "typed", "raw": "dentist", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\ndentist\n```\n"}
{"id": "ph-13", "source": "typed", "raw": "Read the Foundation Models guided-generation docs before starting the iPad prototype", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nRead the Foundation Models guided-generation docs before starting the iPad prototype\n```\n"}
{"id": "ph-14", "source": "typed", "raw": "book flights for", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nbook flights for\n```\n"}
{"id": "ph-17", "source": "typed", "raw": "leak under the bathroom sink getting worse", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nleak under the bathroom sink getting worse\n```\n"}
{"id": "ph-18", "source": "typed", "raw": "Submit the reimbursement for the conference hotel by end of month", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nSubmit the reimbursement for the conference hotel by end of month\n```\n"}
{"id": "ph-20", "source": "typed", "raw": "milk eggs bread coffee filters", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nmilk eggs bread coffee filters\n```\n"}
{"id": "ph-22", "source": "typed", "raw": "car registration and also the smog check", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\ncar registration and also the smog check\n```\n"}
{"id": "ph-23", "source": "typed", "raw": "Idea: a raw-text fallback for capture-parse that never loses the utterance — couldn't parse, kept verbatim", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nIdea: a raw-text fallback for capture-parse that never loses the utterance — couldn't parse, kept verbatim\n```\n"}
{"id": "real-01", "source": "dictated", "raw": "Testing with voice", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nTesting with voice\n```\n"}
{"id": "real-02", "source": "dictated", "raw": "Call the dentist for new aligners", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nCall the dentist for new aligners\n```\n"}
{"id": "real-03", "source": "unknown", "raw": "order clothes", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\norder clothes\n```\n"}
{"id": "real-04", "source": "unknown", "raw": "golf practice", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\ngolf practice\n```\n"}
{"id": "real-05", "source": "unknown", "raw": "Setup hermes agent", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nSetup hermes agent\n```\n"}
{"id": "real-06", "source": "dictated", "raw": "I need to return that Amazon package and also buy printer paper. Maybe stop at the pharmacy", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nI need to return that Amazon package and also buy printer paper. Maybe stop at the pharmacy\n```\n"}
{"id": "real-07", "source": "dictated", "raw": "Okay so I need to pick up the dry cleaning and also get dog food and maybe grab some coffee while I'm at it?", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nOkay so I need to pick up the dry cleaning and also get dog food and maybe grab some coffee while I'm at it?\n```\n"}
{"id": "real-08", "source": "dictated", "raw": "Review the draft contract for the meeting", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nReview the draft contract for the meeting\n```\n"}
{"id": "real-09", "source": "dictated", "raw": "Buy a new USBC cable", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nBuy a new USBC cable\n```\n"}
{"id": "real-10", "source": "dictated", "raw": "Schedule the oil change for the car", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nSchedule the oil change for the car\n```\n"}
{"id": "real-11", "source": "dictated", "raw": "Email Sarah about the", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nEmail Sarah about the\n```\n"}
{"id": "real-12", "source": "dictated", "raw": "Book the the flights for", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nBook the the flights for\n```\n"}
{"id": "real-13", "source": "dictated", "raw": "Pick up the prescription tonight", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nPick up the prescription tonight\n```\n"}
{"id": "real-14", "source": "dictated", "raw": "We're new the car registration by the 15th", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nWe're new the car registration by the 15th\n```\n"}
{"id": "real-15", "source": "dictated", "raw": "Send the revised proposal to James by Friday", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nSend the revised proposal to James by Friday\n```\n"}
{"id": "real-16", "source": "dictated", "raw": "Move the dentist appointment. Actually no reschedule the cleaning to the 21st", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nMove the dentist appointment. Actually no reschedule the cleaning to the 21st\n```\n"}
{"id": "real-17", "source": "dictated", "raw": "Schedule the actually no it reschedule that budget meeting to Tuesday", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nSchedule the actually no it reschedule that budget meeting to Tuesday\n```\n"}
{"id": "real-18", "source": "dictated", "raw": "Remind me to cancel that free trial next Thursday", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nRemind me to cancel that free trial next Thursday\n```\n"}
{"id": "real-19", "source": "dictated", "raw": "Remind me to water the plants when I get home", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nRemind me to water the plants when I get home\n```\n"}
{"id": "real-20", "source": "dictated", "raw": "Remind me to tackle the take the trash. Take the trash cans out tomorrow morning", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nRemind me to tackle the take the trash. Take the trash cans out tomorrow morning\n```\n"}
{"id": "real-21", "source": "dictated", "raw": "To deal with the weird charge on the credit card", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nTo deal with the weird charge on the credit card\n```\n"}
{"id": "real-22", "source": "dictated", "raw": "The thing with the the hotel reservation in Chicago", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nThe thing with the the hotel reservation in Chicago\n```\n"}
{"id": "real-23", "source": "dictated", "raw": "The thing with the garage door sensor", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nThe thing with the garage door sensor\n```\n"}
{"id": "real-24", "source": "dictated", "raw": "Finish the quarterly notes update the server inventory and I still haven't replied to mark", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nFinish the quarterly notes update the server inventory and I still haven't replied to mark\n```\n"}
{"id": "real-25", "source": "dictated", "raw": "Email the photographer confirm the dinner reservation and I haven't yet about a wedding gift", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nEmail the photographer confirm the dinner reservation and I haven't yet about a wedding gift\n```\n"}
{"id": "real-26", "source": "dictated", "raw": "Pick up milk. Replace the air filter and I haven't called the plumber yet", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nPick up milk. Replace the air filter and I haven't called the plumber yet\n```\n"}
{"id": "real-27", "source": "dictated", "raw": "Order more toner send Rachel the updated spreadsheet and I I still haven't submitted the expense report", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nOrder more toner send Rachel the updated spreadsheet and I I still haven't submitted the expense report\n```\n"}
{"id": "real-28", "source": "dictated", "raw": "I should go to the bank and drop off the library books and fill up the car with gas while I'm out", "prompt": "# Capture-parse prompt\n\nThis exact wording is fed to **both** parsers (Gemini Nano on-device and the hosted\nbaseline). Identical prompt is the point of the bake-off — a difference in output is a\ndifference in the model, not the instructions. Do not tune it per parser.\n\nThe runner interpolates the JSON Schema and the raw capture where marked, then requests\nschema-constrained output against that schema.\n\n---\n\nYou turn one raw personal-task capture — dictated or typed, often messy — into a single\nstructured task, conforming exactly to the provided JSON Schema.\n\nRules:\n\n1. **Find the actionable line and make it the `title`.** In clean typed input the title is\n   the whole thing. In run-on dictation you must locate the actions; strip filler (\"okay\n   so\", \"uh\", \"I need to\", \"maybe\"). Keep the title short and imperative. When the capture\n   holds more than one action, the title is the **first action stated** — not the most\n   important or the \"clearest\" one. First is a fact about the utterance; clearest is an\n   opinion, and two parsers will never agree on it.\n2. **Never lose content.** Anything in the raw that is neither the title nor an `items`\n   entry goes into `notes`. If there's no remainder, omit `notes` — don't emit an empty\n   string.\n3. **Do not invent.** Never add a fact, name, date, or field the raw doesn't contain.\n4. **`due` is optional and conservative.** Set it only when the raw carries an explicit\n   temporal phrase. Resolve to `YYYY-MM-DD` if you can; otherwise keep the phrase verbatim.\n   No temporal phrase → `null`. Never guess a date.\n5. **`label` is optional and conservative.** Add a `context`/`energy`/`size` hint only when\n   the raw clearly implies it. Omit any dimension you'd be guessing.\n6. **Multiple actions in one capture:** list **every** action in `items`, in the order\n   spoken, each one imperative and filler-stripped. `items[0]` is the same action as\n   `title`. A single-action capture omits `items` entirely — never emit a one-element list.\n   Anything that isn't a well-formed action (an incomplete clause, a qualifier) stays out\n   of `items` and goes to `notes` instead. This does not mint several tasks: one capture is\n   still one task. Whether these become separate issues is decided later, by a human in\n   Triage — your job is to surface them, not to adjudicate them.\n7. **If you cannot parse it,** put the entire raw text verbatim in `title` and leave the\n   rest unset. A kept-but-unparsed capture beats a lost or hallucinated one.\n\nOutput only JSON conforming to the schema. No prose, no code fences.\n\nSchema:\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://github.com/JddAndrewLauren/hummingbird/experiments/capture-parse-bakeoff/schema.json\",\n  \"title\": \"capture-parse\",\n  \"description\": \"The parse target: one raw capture -> the local store's task-field schema. Core is title+notes (what the sweeper already does). The `due` and `label` fields are STRETCH probes -- the places a small model is most likely to fail -- and are optional by design so the bake-off can measure them separately without penalising a title+notes-only parse.\",\n  \"type\": \"object\",\n  \"required\": [\"title\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"minLength\": 1,\n      \"description\": \"The single actionable line, found within the raw capture. On a clean typed capture this is verbatim; on messy dictation the parser must LOCATE it. Never invent content that isn't in the raw.\"\n    },\n    \"notes\": {\n      \"type\": \"string\",\n      \"description\": \"Everything else worth keeping that is NOT an action -- qualifiers, context, incomplete clauses, and any note about a deviation from the raw (a cleaned stutter, a suspected mis-transcription). Omit the field entirely when there is no remainder (do not emit an empty string). Nothing from the raw may be silently dropped: content that is neither the title nor an `items` entry belongs here.\"\n    },\n    \"items\": {\n      \"description\": \"Every action found in the capture, in the order spoken, imperative and filler-stripped. Present ONLY when the capture holds more than one action; omit it entirely for a single-action capture. `items[0]` is always the same action as `title` -- the redundancy is deliberate, so scoring never special-cases the promoted one. A quarter of real dictated captures are multi-item, and burying the 2nd and 3rd actions in `notes` prose makes them invisible to the system while looking lossless; this field keeps them discrete and countable. It does NOT mint several tasks: one capture is still one task, and whether these become separate issues is a Triage decision, not a parse decision.\",\n      \"type\": \"array\",\n      \"minItems\": 2,\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      }\n    },\n    \"due\": {\n      \"description\": \"STRETCH. An inferred due date, only when the raw carries an explicit temporal phrase (\\\"by Thursday\\\", \\\"the 15th\\\", \\\"tonight\\\"). ISO-8601 date (YYYY-MM-DD) if resolvable, else the verbatim phrase. null when nothing temporal is present -- do NOT guess. The sweeper deliberately drops due dates; this field exists to measure whether on-device inference is safe enough to change that.\",\n      \"type\": [\"string\", \"null\"]\n    },\n    \"label\": {\n      \"description\": \"STRETCH. Coarse GTD hints, each optional; omit any dimension not clearly implied by the raw. Probes whether a small model can add structure or only hallucinates it.\",\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"context\": {\n          \"type\": \"string\",\n          \"description\": \"Where/with-what the action happens, e.g. @home, @errands, @phone, @computer.\",\n          \"enum\": [\"@home\", \"@errands\", \"@phone\", \"@computer\", \"@work\", \"@anywhere\"]\n        },\n        \"energy\": {\n          \"type\": \"string\",\n          \"description\": \"Rough energy the action demands.\",\n          \"enum\": [\"low\", \"medium\", \"high\"]\n        },\n        \"size\": {\n          \"type\": \"string\",\n          \"description\": \"Rough size; `deep` is the action ceiling (see CONTEXT.md).\",\n          \"enum\": [\"quick\", \"medium\", \"deep\"]\n        }\n      }\n    }\n  }\n}\n```\n\nRaw capture:\n\n```\nI should go to the bank and drop off the library books and fill up the car with gas while I'm out\n```\n"}
