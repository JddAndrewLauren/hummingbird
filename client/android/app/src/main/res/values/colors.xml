<resources>
    <!-- sand-50: the light plate, tokens/colors.css — the main launcher
         icon's background, matching the light-plate bird artwork it wraps
         (client/web/public/app-icon-512.png, the repo's only brand mark). -->
    <color name="ic_launcher_background">#fdfaf6</color>
    <!-- ink-700 (#2f3a45): "the dark plate" per the design README, sampled
         directly off client/web/src/design/brand/app-icon-dark-78.png's own
         corner pixels — NOT ink-950/theme-color (#0f141a), a different,
         darker token that would leave a visible seam around the artwork's
         own plate. Matches the dark-plate bird art it wraps, so the two
         launcher entries are tellable apart at a glance. -->
    <color name="ic_launcher_capture_background">#2f3a45</color>
</resources>
