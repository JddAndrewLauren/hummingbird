[workspace]
resolver = "2"
members = [
    "domain",
    "authority",
    "city-waste",
    "gmail-poll",
    "graph-poll",
    "rules-engine",
    "worker",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
