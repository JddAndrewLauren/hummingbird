{
  "_comment": "Google Tasks lists the sweeper must never touch, keyed by list id with the human-readable title as the value (ids are rename-proof; titles alone break silently). Seed new entries from `./sweep.py --dry-run` output, which logs `list id=<id> title='<title>'` for every list. An unknown or stale id fails open: that list gets swept, never skipped.",
  "VzVwaWhkclQ1MW1yNE5qQw": "Shopping"
}
