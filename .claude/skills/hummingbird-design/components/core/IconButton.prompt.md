A square, label-less control for toolbars, row affordances and app bars.

```jsx
<IconButton icon="refresh-cw" label="Refresh calendar" />
<IconButton icon="bell" label="Alerts" active />
```

`label` is required. Use `size="lg"` (44px) on any touch surface.
