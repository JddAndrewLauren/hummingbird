Native select with Hummingbird chrome and a Lucide chevron.

```jsx
<Select label="Stage" value={stage} onChange={e => setStage(e.target.value)}
  options={["Triage","Grilling","Ready","In Progress","Blocked","Done"]} />
```
